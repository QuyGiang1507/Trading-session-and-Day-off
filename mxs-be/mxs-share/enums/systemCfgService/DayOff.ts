export enum FixedDayOffChannels {
    Create = "fixed-day-off-created",
    Update = "fixed-day-off-updated",
}

export enum UnfixedDayOffChannels {
    Create = "unfixed-day-off-created",
    Update = "unfixed-day-off-updated",
}

export enum DayOffStatus {
    PENDING = "pending",
    APPROVED = "approved",
    REJECTED = "rejected",
} 