export enum CommodityChannels {
  Create = "merchandise-commodity-created",
  Update = "merchandise-commodity-updated",
}

export enum CommodityStatus {
  ACTIVE = "active",
  INACTIVE = "inactive",
}
